package com.finanza.desktop.ui;

import com.finanza.desktop.model.Usuario;
import javax.swing.*;

/**
 * Tela de movimentações/transações (stub)
 */
public class MovementsFrame extends JFrame {
    public MovementsFrame(Usuario usuario) {
        setTitle("Movimentações");
        setSize(800, 600);
        setLocationRelativeTo(null);
        
        // Implementação temporária
        JLabel label = new JLabel("Funcionalidade em desenvolvimento");
        add(label);
    }
}