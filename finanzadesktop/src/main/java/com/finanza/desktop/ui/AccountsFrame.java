package com.finanza.desktop.ui;

import com.finanza.desktop.model.Usuario;
import javax.swing.*;

/**
 * Tela de gerenciamento de contas (stub)
 */
public class AccountsFrame extends JFrame {
    public AccountsFrame(Usuario usuario) {
        setTitle("Gerenciar Contas");
        setSize(800, 600);
        setLocationRelativeTo(null);
        
        // Implementação temporária
        JLabel label = new JLabel("Funcionalidade em desenvolvimento");
        add(label);
    }
}